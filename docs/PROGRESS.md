Warning: truncated output (original token count: 1362400)
... 4401021 bytes omitted ...

- **종결(2026-09-06 10:2X UTC · sess-1013-22734 · 워커 E · 계정 2 · lock 인계)**: 원 세션의 lock(08:48:49)이 90분을 넘겨(10:18 경과) 규약대로 인계. 확인한 것 — `git fetch origin screens` → 브랜치 파일 = `.nojekyll` + PNG 26장(01_lobby~26_arena_shop · 이름 = docs/ref 번호) + `layout.json` + `meta.json`(`{sha: d399e3f…, run: 83, run_id: 34025849425, utc: 09:58:49Z}`) · `layout.json` 키 26 화면 + `_missing: []` + `_meta{frame 1080×2337 · shot 540×1168 · unity 6000.3.8f1}` · `python3 tools/ui_score.py <화면> layout.json` 이 ⑤·⑦·⑨~㉔ 표를 찾아 행별 대조표를 냄(회차 1 실사용 22 화면 · 이 세션이 12·11·15~19 를 채점). **§2 T46 1~6 전부 성립** — 남은 결함은 촬영(UI 가 PNG 가운데 34.8% 띠 · 월드 스프라이트 겹침)뿐이라 T58 로 분리돼 있다(눈 비평은 `tools/png_crop.py` 로 대체 중). T47(로비·전투 회차)·T58 은 이 종결로 열린다.
# PROGRESS — 꼬마기사 키우기 유니티 이식 (aaawunity)

> 갱신 규약은 `docs/ROUTINE.md` §4. 스펙은 aaaw `PLAN.md`(읽기 전용 · 변경 금지). 수치 정본은 aaaw `data/*.json`.

## 작업 상태

| ID | 작업 | 상태 | SID / 워커 | 범위 | 핵심 |
|---|---|---|---|---|---|
| T519 | 치와와 UI 아트 제작·런타임 연결 및 클라우드 세션 분담 | ✅ 완료 — 계획 아트 전량 검수·카탈로그·실제 연결; CI 34783274626 EditMode 596/596 + PlayMode 258/258; screens 58장 직접 검수 | chatgpt-work-cloud-t519 | `docs/ChihuahuaGameUI-Sessions.md`, `tools/game_ui_art/`, `Assets/Art/ChihuahuaGameUI/`, 상세 TODO의 런타임 연결 및 카탈로그 | `f871456b`; T515~T518·T513 장비 27세트 보존 |
| T518 | 👀 **주인 폰 확인에서 나온 UI 결함 묶음**(보스 알림이 «반쪽만 검정 · 점점점» · 판매 버튼 글자에 네모 · 게임 곳곳 글자에 네모 · 비교 팝업 상·하 칸 크기가 옵션 수에 따라 바뀐다 · UI 비율 틀린 곳 많다 · 9:19 화면에서 9:16 전용처럼 보인다) | ⬜ **대기(등재만) — 자리를 못 짚은 것이 있어 주인 그림 한 장이 필요하다(2026-09-13 · 원격 세션)** ▸ **짚은 것**: 옵션 줄 상한(T515 · 닫음) · 다이아 아이콘 통일(T514 · 닫음). ▸ **못 짚은 것**: «판매 버튼» 은 코드·프리팹·표 어디에도 «판매» 글자가 **0건**이다(다른 이름의 버튼을 말씀하신 듯) · «보스 알림» 은 T235 로 **띠를 이미 뺐고** 지금 보스 등장에서 도는 것은 곡 하나뿐이다 · «네모»(두부 글자)는 CI 그림 52장에서 **한 개도 안 잡힌다**(폰에서만 나는 것 = 동적 글리프 굽기·아틀라스 쪽 의심) · «비율»·«9:19» 는 어느 화면인지 가리켜야 잰다. | 원격(주인 직접 지시) | 미정 | 폰 그림 한 장이면 바로 짚는다 |
| T517 | 🎲 **주인 «장비·펫·탈것 옵션을 아홉 가지로 바꿔줘 기존꺼 버리고»**(공격력 1~10% · 치명타확률 1~8% · 치명타데미지 1~50% · 체력 1~10% · 더블어택 1~10%(라운드당 1회) · 스킬데미지 1~10% · 흡혈 1~10% · 회피 1~3% · 라운드당 체력회복 1~3%) | ⬜ **대기(등재만) — 아직 한 줄도 안 고쳤다(2026-09-13 · 원격 세션)** ▸ 지금 옵션은 `gear.json`(aaaw 정본)의 **종류마다 고정 7줄**이다. 주인 표는 **범위에서 굴리는 아홉 축**이라 «아이템마다 굴린 값을 들고 다니는» 꼴로 바뀐다 — 세이브 형식이 바뀌고(굴린 값 저장) 이관이 필요하다. ▸ 아홉 중 셋(**더블어택 · 스킬데미지 · 라운드당 회복**)은 **T516 턴제**가 서야 뜻이 생긴다 ⇒ **T516 뒤에 한다**. ▸ «탈것» 은 이 레포에 아직 없는 계통이다(펫은 있다) — 그 표까지 주인이 준 것이라 T516·T517 을 하며 자리만 비워 둔다. | 원격(주인 직접 지시) | `Core/GearSystem`·`SaveData`·표 새로 · 화면 다수 | 옵션 아홉 축 · 범위 굴림 · T516 뒤 |
| T516 | 🔁 **주인 «싸움을 턴제 게임으로 바꿀래» — 1웨이브 15라운드 · 내가 먼저 때리고 적이 때리고 · 스킬은 쿨타임 대신 3라운드당 1회 · 1웨이브당 1마리** | ⬜ **대기(등재만) — 아직 한 줄도 안 고쳤다(2026-09-13 · 원격 세션)** ▸ **왜 등재로 두나**: 이것은 «엔진 교체» 다 — `Core/Battle.cs` 의 실시간 틱(1/30초 · sim.js 이식 · T2 시드 골든이 지키는 그 규칙)을 **라운드 루프**로 바꾸는 일이고, 그 순간 `BattleTests` 의 골든 아홉·`Sim` 하니스·`BalanceLadder`·전투 화면 연출이 전부 옛 규칙을 가리킨다. ▸ **주인에게 물었다가 접혔다**(주인이 물음을 닫고 다음 지시를 줬다) — 그래서 아래를 **가정으로 못 박고** 간다. 다르면 주인 한마디에 바꾼다: ⓐ 공격속도는 무시(라운드마다 나 1대 · 적 1대) · ⓑ 15라운드 초과 = 지금 «죽었다» 와 같은 흐름(부활권 → 결과) · ⓒ 이동은 남긴다(걸어가 만나면 라운드 시작) · ⓓ «3라운드당 1회» 는 발동형 전부(특전·장비·펫). ▸ **딸린 것**: 웨이브당 적 1마리(노드 표는 그대로 두고 세우는 수만 1) · 라운드 표시 UI · «15라운드» HUD. | 원격(주인 직접 지시) | `Assets/Scripts/Core/Battle.cs` 중심 · 전투 화면·자 다수 | 실시간 → 턴제(라운드) · 아직 시작 안 함 |
| T515 | 🎛 **주인 «옵션은 최대 2개로 해줘야함 장비들»** | ✅ **종결 — 한 회차(2026-09-13 · 원격 세션 · 결정 1415)** ▸ 표 두 칸: `optCount` [0,1,2,2,**2**] · `mythPlusAt` **[]**(강화로 줄이 더 열리던 사다리를 걷었다). 사이버 3 → 2 · 강화로 열리던 +3/+6/+9/+12 네 줄은 사라진다. ▸ **그리는 쪽도 같이** — `GearUi.OptionRows` 가 정본 7줄을 다 그리고 있었다(못 여는 줄은 자물쇠로). 상한을 넘는 줄은 **영영 안 열리므로 안 그린다**(`GearData.OptCountOpenMax` 새로 세움). ▸ 자 여섯이 옛 사다리(신화 3 · +12 에 7줄)를 지키고 있어 **뒤집었다**(EditMode 넷 · PlayMode 둘). | 원격(주인 직접 지시) | `Assets/KkomaKnight/gearOverride.json` · `Core/GameData.cs`(OptCountOpenMax) · `Game/GearUi.cs` · 자 여섯 | 어느 장비도 옵션 줄 2개를 넘지 않는다 |
| T514 | 💎 **주인 «아레나 부분에 상점에 다이아 파는거가 메인 상점에서 파는 다이아랑 모양이 다른데? 통일시켜야지 메인 상점꺼로»** | ✅ **종결 — 한 회차(2026-09-13 · 원격 세션 · 결정 1414)** ▸ **실측**: 재화 «다이아» 그림을 코드가 두 가지로 쓰고 있었다 — 상단 바·메인 상점 값·펫 소환·던전 입장권·우편은 `hud.gem`(ResourceBar_Icon_Gem)인데 **아레나 화면만** `ui.iconGemPurple`(Economy_Gem_02_Purple)이었다. 주인이 본 그 차이다. ▸ **고침** — 아레나 상인 상품 칸과 보상 칸(`EventsScreen.Goods` · `RewardArt`) 둘 다 `hud.gem` 으로. 새 그림 0 · 다른 화면은 손 안 댔다(이미 그 그림이다). ▸ ⚠ **안 바꾼 것 하나** — 순위 보상 «티어 띠» 의 «다이아»(브론즈·실버·골드·플래티넘·**다이아**)는 <b>등급 이름</b>이지 재화가 아니라 그 자리의 `ui.iconGemPurple` 은 그대로 뒀다. | 원격(주인 직접 지시) | `Assets/Scripts/Game/EventsScreen.cs` · `Assets/KkomaKnight/catalog.json`(설명) | 재화 다이아 그림은 어디서나 `hud.gem` 한 가지 |
| T513 | 주인 요청: 9등급 × 도적·전사·암살자 치와와 장비 아트 27세트 | ✅ 완료 — 원본 PNG 27·PSB 27·투명 썸네일 162 | sess-chihuahua-9fc4baf5fd9a · Codex | `Assets/Art/ChihuahuaEquipmentThemes/**` · `tools/chihuahua_art/**` | 같은 캔버스·8레이어 순서/ID, 무기 길이 오차 ≤2px. PSB 재열기·원본 합성·162개 RGBA 썸네일·Unity 메타 검사 통과. C# 빌드 0경고/0오류·프로젝트 정적 검사·데이터 동기화·Sim 통과. 로컬 VSTest 실행기 예외로 단위 테스트 결과 없음; Unity 에디터 직접 임포트 미실행. 주인 승인 범위로 main에 소스 아트 추가; 본/웨이트·런타임 장비 연결은 포함하지 않음. |
| T512 | ↩ **주인 «플레이어는 항상 왼쪽에 원래 있던 위치에 카메라로 고정되있어야함 · 플레이어가 카메라로 봤을 때 오른쪽 왼쪽 이렇게 왔다갔다 ㄴㄴ»** | ✅ **종결 — T510 을 통째로 되돌렸다(2026-09-13 · 원격 세션 · 결정 1412)** ▸ 오늘 아침 T510 이 «플레이어가 오른쪽으로 걸어가고 맵이 선다» 로 고쳤는데, 주인이 폰에서 보고 **그 오감(16%↔62%)을 거부**했다. 카메라 띠·행군 따라붙기를 없애고 **원점 = 플레이어 x**(옛 꼴)로 되돌린다 — 그림은 늘 `ui.json camera.playerX`(16%) 한 자리다. ▸ **되돌린 방법** — 새로 만든 것은 지우고(`Core/BattleCam.cs` · `Tests/EditMode/BattleCamTests.cs`) 만졌던 셋은 T510 직전 커밋(`edd533ce`)의 것으로 되돌렸다(`Game/BattleWorld.cs` · `Core/Layout.cs` 상수 둘 · `Tests/PlayMode/PvpStageTests.cs`). 그 자의 챕터 갈래는 **원래 «플레이어가 붙박여 있다» 를 재던 자**라(T319) 되돌리자마자 주인이 지금 시킨 것을 그대로 지킨다. ▸ ⚠ **T510 의 «맵이 왼쪽으로 흐른다» 는 되돌아온다** — 둘은 같은 한 줄의 앞뒤라 동시에 만족시킬 수 없다(무한히 오른쪽으로 가는 판 + 유한한 화면). 주인 마지막 말이 «고정» 이므로 고정이 답이다. | 원격(주인 직접 지시) | `Assets/Scripts/Game/BattleWorld.cs` · `Assets/Scripts/Core/Layout.cs` · `Assets/Scripts/Core/BattleCam.cs`(지움) · `Assets/Tests/EditMode/BattleCamTests.cs`(지움) · `Assets/Tests/PlayMode/PvpStageTests.cs` | T510 되돌림 — 플레이어는 화면 16% 에 고정 |
| T511 | 🏷 **주인 «원시·중세·근대·현대·사이버·미래·우주·불멸·무한 으로 등급이름 바꿔주고 · 상점에도 그거 이름 반영 · 무기 이름도 사이버 등급이면 «사이버 암살자의 무기» 이런 식으로 · 장비 이름 죄다»** | ✅ **종결 — 한 회차(2026-09-13 · 원격 세션 · 결정 1411)** ▸ **아홉 이름이 이미 있던 아홉 칸에 그대로 맞았다** — 장비 등급 다섯(`gearOverride.json rarName`)과 그 위 표시 등급 넷(`gearTier.json` · T316)을 이어 붙이면 **정확히 9칸**이고, 주인 목록의 끝 둘(불멸·무한)이 지금 표의 끝 둘과 **같은 글자**다. 그래서 순서대로 얹었다: 일반→원시 · 희귀→중세 · 영웅→근대 · 전설→현대 · 신화→**사이버** · 갓→**미래** · 초월→**우주** · 불멸·무한은 그대로. ▸ **상점**(주인 «상점에도») — 상자 이름은 aaaw 정본 `data/gacha.json` 에 있어 못 고치므로 `gachaOverride.json` 에 **name 칸을 열어** 덮었다(희귀→**중세 상자** · 전설→**현대 상자** · 신화→**사이버 상자** · 이름이 붙던 규칙 «그 상자의 최고 등급» 그대로). 업적 표의 «… 상자 5회 오픈» 석 줄도 같이 간다. ▸ **장비 이름**(주인 «죄다») — `GearUi.Name` 이 앞에 **그 장비가 띄우는 등급**을 붙인다: «사이버 암살자의 무기» · 사이버 +3 이면 카드가 «미래» 로 뜨므로 «**미래** 암살자의 무기». 대장간 두 줄이 등급을 또 붙이고 있어 뗐다(안 그러면 «사이버 사이버 …»). ▸ **수치 0줄** — 칸 수·기여·확률·색·키는 한 자도 안 바뀌었다(`dotnet test` 587개 초록). | 원격(주인 직접 지시) | `Assets/KkomaKnight/`(gearOverride·gearTier·gachaOverride·achievement·catalog) · `Core/GameData.cs`(상자 name 덮기) · `Game/GearUi.cs`·`Game/ForgeScreen.cs` · 자 일곱 | 등급 이름 아홉 = 원시·중세·근대·현대·사이버·미래·우주·불멸·무한 · 상자·장비 이름이 그 이름을 따른다 |
| T510 | ↩ **되돌려졌다(T512 · 주인 2026-09-13 «플레이어는 항상 왼쪽에 고정») — 아래는 이력이다.** 🎥 **주인 «그 플레이어가 실제로 오른쪽으로 가면서 게임이 진행되야하는데 맵이랑 적들이 왼쪽으로 움직히는 방식 이더라 · 해결하기»** | ✅ **종결 — 한 회차(2026-09-13 · 원격 세션 · 결정 1410)** ▸ **까닭은 한 줄이었다** — 챕터 판의 그리기 원점이 **플레이어 월드 x 그 자체**(`LayoutX = (worldX − _shownPX)×zoom + playerX`)라 그림이 `ui.json camera.playerX`(16%) 에 **붙박이고** 세상만 흘렀다. T319 가 아레나 판에서 같은 병을 이미 진단해 놓고(«내가 오른쪽으로 가는 느낌이 아니라») **챕터 판은 그대로 못 박아 뒀다** — 그 못을 뽑았다. ▸ **고침** — 원점을 카메라로 떼어 냈다(`Core/BattleCam.cs` · 규칙 셋: ① 플레이어를 앞지르지 않는다 ② 띠의 오른쪽 끝(`Layout.BattleCamRightPct` 62%)에서 밀린다 ③ **행군에서만** 걸음의 `BattleCamCatchUp`(1.5)배로 따라붙어 왼쪽 끝으로 되돌린다). 무리 안의 걸음은 «싸움» 이라 **원점이 한 톨도 안 움직인다** = 맵·적이 서고 내가 걸어간다. 싸움/행군은 «다음 적까지의 거리»(멈춤 74 + 적 간격 44×1.5 = 140 월드 px · `combat.json`)로 가른다. ▸ **엔진 0줄** — 좌표·틱·시드·수치를 한 줄도 안 봤다(`dotnet test` 595개 초록 · 시드 골든 그대로). ▸ **실측(EditMode 자가 실제 전투를 돌려 센다)** — 서 있는데 맵이 흐른 프레임 **0** · 원점이 뒤로 간 프레임 **0** · 그림이 오간 폭 **16%~62%**(옛 꼴은 폭 0) · 무리 안 걸음 중 맵이 선 채로 간 몫 ch1 **96%** · ch10 **85%** · ch30 **66%**(오른쪽 끝에 닿은 뒤만 밀린다). ▸ **눈으로도 봤다** — 같은 시드·같은 전투를 옛 규칙/새 규칙으로 돌려 진짜 맵 스프라이트 위에 그린 필름 스트립(주인에게 보낸 그림 · 커밋 안 함). | 원격(주인 직접 지시) | `Assets/Scripts/Core/BattleCam.cs`(새것) · `Assets/Scripts/Core/Layout.cs`(상수 둘) · `Assets/Scripts/Game/BattleWorld.cs` · `Assets/Tests/EditMode/BattleCamTests.cs`(새것) · `Assets/Tests/PlayMode/PvpStageTests.cs`(챕터 쪽 자를 뒤집었다) | 원점 = 플레이어가 아니라 **카메라** · 싸울 땐 맵이 서고 내가 오른쪽으로 간다 · 행군에서만 카메라가 앞을 비춘다 |
| T509 | ⛑ **규약 27행 «두 작업이 같은 파일을 만져야 하면 뒤 번호가 기다린다» 를 지키는 자가 아무 데도 없다 — 살아 있는 lock 둘이 «범위» 칸에 같은 파일을 정직하게 적어도 아무것도 안 운다** | ✅ **종결 — 한 회차 · lock 반납(sess-1424-31894 · 워커 B · 17:2X · 결정 1407·1409)** ▸ **확인을 런의 «제 말» 로 읽었다** — 런 **1169**(`2bb34688`) `dotnet` 잡에 **#39 «살아 있는 lock 이 «범위 열에 없는 파일» 을 쥐고 있는가 — 알리기만 (T347)»** 과 **#40 «선점 범위 자 자기 검사 (T347)»** 가 저마다 **이름으로 서고 success**(잡이 초록인 것으로 갈음하지 않았다 · 결정 1348). ▸ ⚑⚑ **그런데 «이름으로 섰다» 만으로는 이 절이 안 닫힌다** — 그 단계 이름은 **내 커밋 전에도 있었다**(T347 이 놓은 것이라 `ci.yml` 0줄로 실렸다). 이름만 보면 «옛 7갈래가 돌았다» 와 «새 15갈래가 돌았다» 가 **한 글자다**(T278). 그래서 **로그를 열어 그 자가 스스로 적은 수를 읽었다**: `16:49:05 ✓ check_claim_scope 자기검사 **15**칸` · `16:49:05 ✓ check_claim_scope: 살아 있는 lock 2개 · 범위 열이 실제로 여는 파일을 다 적는다`. ⇒ 15 가 글자로 섰고(7 이 아니다), 그 시각 살아 있던 둘(`T507`·`T509`)은 범위가 안 겹쳐 ⓓ 는 **옳게 조용했다**. ▸ **건진 것 하나(결정 1409)** — T505·T506 은 갈래 수를 **단계 이름**(`ci.yml`)에 적어 이 문제를 풀었다. 이 절은 `ci.yml` 을 «0줄» 로 적어 그 길이 막혔는데, **자기 검사가 제 출력 줄에 수를 적고 있어서** 로그 한 줄로 같은 답이 났다. ⇒ **갈래 수는 두 자리 중 어디에 적어도 되지만, 어느 한쪽에는 있어야 한다.** ▸ ⚠ **안 쟀다**(결정 1106 ②): ⓐ 이 갈래가 **진짜 겹침이 살아 있는 CI 런**에서 우는 것은 아직 못 봤다(런 1169 때는 겹침이 없었다 — 그 증명은 1회차에 사고 시각의 실물로 따로 했다) · ⓑ 유니티 잡은 이 런에서 **건너뛰었다** — `has_code == 'false'`(T433 의 잣대)라 설계대로다(내 커밋은 `tools/`·`docs/` 뿐이다) · ⓒ 확장자 없이 적은 범위는 여전히 못 본다(갈래 ⓚ 의 한계). ‖ (이력) 🔄 **1회차 push(17:0X · 결정 1407)** ▸ **세운 것 — `check_claim_scope` 에 갈래 ⓓ**: 살아 있는 lock 들의 «범위» 칸을 서로 맞대어 같은 파일을 적은 쌍을 이름과 함께 찍고 «뒤 번호가 기다린다» 를 말한다. **`ci.yml` 0줄** — `:217`(본 검사)·`:219`(`--selftest`)가 이미 걸려 있어 새 갈래가 그 자리에 그냥 실린다(새 단계·새 규약 0 · 결정 699 ③). ▸ ⚑ **첫 실물이 바로 이 자를 한 번 속였다** — T507 행의 «**`ci.yml` 0줄**(`T506.lock` 의 범위라 안 연다)» 에서 `ci.yml` 을 **제 범위로** 읽어 겹침을 둘로 셌다. 범위 칸은 산문이라 «안 연다» 도 그 이름을 적는다 — **뜻이 정반대인데 글자가 같다.** 그래서 «이름 바로 뒤에 0줄·안 연다·안 본다» 가 붙으면 빼는 걸음(`NOT_MINE`)을 넣었다. ⇒ **잡으라고 세운 바로 그 한 건이 그 자의 첫 거짓을 꺼냈다**(T505·T506 이 «자기 검사는 지키는 것보다 아는 것을 먼저 준다» 로 적은 그 꼴). ▸ **표본이 장치를 지나는 것을 봤다**(결정 1404 ③ⓐ) — 지어낸 칸이 아니라 **사고 그 시각의 실물**로 돌렸다: `43a88615` 의 `PROGRESS.md` + 그때의 `T507.lock`·`T508.lock` 을 스크래치에 풀고 «지금» 을 16:26:26Z 로 고정하니 «살아 있는 lock 둘이 같은 파일을 제 범위로 적었다 — T507 ↔ T508 · 겹친 파일 `check_stale_asserts.py` · 기다리는 쪽은 T508» 이 나온다. `ci.yml` 은 안 딸려 온다. **오늘 이 자가 20분 내내 못 한 말이 그 한 줄이다.** ▸ **일부러 부러뜨렸다(T249) — 다섯 번**: 맞대기 빼기 → ⓗ·ⓛ · `NOT_MINE` 빼기 → ⓗ·ⓝ · 확장자 없는 이름도 세기 → ⓚ·ⓚ²·ⓜ·ⓝ · 쪼갠 lock 안 합치기 → ⓙ · 이름을 지어내기 → 여덟 갈래. **저마다 다른 갈래가 물렸다.** 갈래 7 → **15** 전부 통과. ▸ **일부러 안 한 것** — `ci.yml:219` 의 단계 이름에 갈래 수(«15»)를 안 적었다. T505·T506 은 그 수를 이름에 적었지만 그러려면 `ci.yml` 을 여는데 **이 절이 제 범위를 «0줄» 로 적어 두었다**. 이름은 이미 있어 확인은 그 이름으로 된다. ▸ ⚠ **한계를 못 박았다**(갈래 ⓚ) — 확장자 없이 적은 범위(`Game/GearUi`)는 ⓓ 가 **안 센다**. 한 방향 물음인 ⓐ 는 그것을 «덮는다» 로 보지만, **두 칸을 서로 맞대는** 물음은 그 토막이 한 파일인지 한 폴더인지 모른다. **고침이 아니라 한계다.** ▸ ⚠ **막는 자로 안 바꿨다** — 이 자는 T347 이 «조율 결함은 알리기만» 으로 세웠다(결정 493·627 · T238). rc 갈래 0줄. **확인** = 다음 완주 런 `dotnet` 잡에 «선점 범위 자 자기 검사 (T347)» 단계가 **이름으로** 서고 초록. ‖ (이력) 🔄 **등재·선점(16:3X)** — 등재만. ▸ **왜 지금 — 오늘 16:2X 에 실제로 났고, 자는 그동안 한 마디도 안 했다.** `T507`(워커 E)과 `T508`(워커 J)이 **5분 34초 차로 둘 다 살아 있고** 두 행의 «범위» 칸이 같은 파일(`tools/check_stale_asserts.py`)을 적었다. 규약은 그 갈래를 이미 정해 뒀다(`docs/claims/README.md` 27행). ▸ **사람이 풀었다** — 워커 E 가 16:4X 에 스스로 알아채고 T508 을 `✂` 로 접었다(push 시각으로 갈랐다). 그 손이 옳았고, **이 절은 그 고침을 되풀이하지 않는다**(결정 846). ▸ ⚑ **이 절이 집는 것은 그 옆자리다 — 겹침이 살아 있는 동안 `check_claim_scope` 를 돌려 «살아 있는 lock 3개 · 범위 열이 실제로 여는 파일을 다 적는다» 로 초록인 것을 봤다.** 이번엔 임자가 알아챘지만 그것은 **운**이다. T508 을 집은 손도 절차 ①(상태 칸이 ⬜ 인가)을 정직하게 지켰다 — 절차에 «남의 살아 있는 lock 의 범위 칸도 읽어라» 가 없을 뿐이다. ▸ **왜 `check_claim_scope` 가 못 보나(두 겹)** — ⓐ 그 자가 잡는 것은 «**안 적힌** 파일을 쥔 lock» 이다(ⓑ→ⓒ). 여기는 **둘 다 적었으므로** ⓑ 가 안 서고 따라서 ⓒ 도 안 선다. ⓑ 를 안 지나고 바로 «살아 있는 lock 끼리 범위를 맞대는» 갈래가 없다. ⓑ 그 자의 «보는 칸» 이 `Assets/Scripts/**/*.cs` 뿐이라 `tools/*.py`·`ci.yml` 은 처음부터 안 본다(머리글에 적힌 사각지대). 오늘 부딪힌 파일이 정확히 그 사각지대 안이다. ▸ **이 회차가 할 것** — 그 자에 갈래 하나를 보탠다: **살아 있는 lock 들의 «범위» 칸을 서로 맞대어 같은 파일을 적은 쌍을 찾고, 규약대로 «뒤 번호가 기다린다» 를 이름과 시각과 함께 찍는다.** ⚠ **막는 자로 바꾸지 않는다** — 이 자는 T347 이 «조율 결함은 알리기만»(결정 493·627 · T238)으로 세운 **알리는 자**다. 그 결정을 뒤집는 것은 이 절의 일이 아니다. ▸ **집는 손(=나)이 지킬 것** — `main()` 을 `run()` 과 나눈다(`sys.argv` 되읽기 · T505 가 밟았다) · `--selftest` 가 이미 있으므로 **거기에 갈래를 보탠다**(새 규약 0 · 결정 699 ③) · **공허 방지**(«lock 을 하나도 못 읽었다» 를 «겹침 0» 과 가른다 · 결정 1379) · **일부러 부러뜨려**(T249) 갈래마다 다른 줄이 물리는 것을 본다 ⚠ **표본이 재려던 장치를 실제로 지나는지 보라**(결정 1404 ③ⓐ). ▸ ⚠ **한계를 미리 못 박아 둔다** — 범위 칸은 **글자로** 견주므로(그 자의 머리글) «그리는 18곳» 같은 **뭉뚱그린 범위는 이 갈래에서도 아무 파일도 안 덮는다**. 그것을 고치는 것은 이 절이 아니다. | sess-1424-31894 · 워커 B | `tools/check_claim_scope.py` 한 파일 — **`ci.yml` 0줄**(그 자는 이미 걸려 있다 · `:217` 본 검사 · `:219` `--selftest`) · 게임 코드 0줄 · `Assets/` 0파일 · `data/*.json` 0줄 · `aaaw` 0줄 | ROUTINE §2 T509 · **T347**(이 자를 세운 절) · **T507·T508**(오늘 부딪힌 둘) · **T238**(조율 결함은 알리기만) · 결정 493 · 결정 627 · 결정 846 · 결정 1379 · 결정 1407 |
| T508 | ⛑ **T492 가 센 «자기 검사 없는 자 다섯» 의 마지막 하나 — `check_stale_asserts`(알리는 자)** | ✂ **번호를 태운다 — 같은 파일·같은 병을 `T507` 이 먼저 잡아 이미 끝냈다(sess-1913-2015 · 워커 E · 16:4X)** — **시각으로 갈랐다**: `T507.lock` push **16:17:19Z**(`24775025`) ↔ `T508.lock` **16:22:53Z**(`43a88615`) · **5분 34초** 먼저다. 규약 그대로 «push 가 먼저 성공한 쪽이 이긴다 · 늦게 민 쪽이 제 번호를 옮긴다»(`docs/claims/README.md`). 행은 **안 지운다** — 지우면 508 이 되살아난다(T284·T296 꼴). **`T508.lock` 은 워커 J 의 것이라 내가 안 지운다**(남의 lock · 90분 규약) — 임자가 반납한다. ▸ ⚑ **그러나 이 등재 글은 헛되지 않았다 — `T507` 이 여기 적힌 둘을 그대로 받았다**: ① **공허 방지**(«아무것도 안 읽었다» 를 «0건» 과 가른다 · 결정 1379) → 갈래 **ⓗ**(«0건» 줄이 «훑은 수» 를 같이 말하는가) ② **«이 자만의 조심 · 한계를 못 박는 갈래»**(오탐이 나는 꼴을 그대로 적어 둔다) → 갈래 **ⓘ**(흔한 수가 상한을 넘으면 **안 찍고** · 한 자리면 **찍되 판정은 안 한다**). «**막는 자로 바꾸지 마라**» 도 지켰다 — 판정 rc **0줄**(결정 493). «표본이 재려던 장치를 지나는지 보라»(결정 1404 ③ⓐ)도 따랐다: 부러뜨림 일곱이 **저마다 다른 조건 이름**을 찍는 것까지 봤다. ⇒ **늦은 번호였지만 그 글이 갈래 둘을 만들었다.** ▸ 남은 것은 `T507` 행에서 읽는다. ‖ **⊕ 늦게 민 쪽의 한 줄 — 내 잘못이 뚜렷하다(sess-1917-23930 · 워커 J · 16:5X · `T508.lock` 반납 · 결정 1406)** ▸ 선점 절차 ①은 «잡을 작업의 **상태 칸**을 읽어라» 다. 나는 그것을 읽었다(T508 은 내가 방금 ⬜ 로 세운 행이라 당연히 ⬜ 였다). **안 읽은 것은 «살아 있는 남의 lock(T507)의 «범위» 칸»** 이고, 거기에 `tools/check_stale_asserts.py` 가 **글자로** 적혀 있었다(16:17 부터 · 내 lock 보다 5분 34초 앞). ⚑ **두 물음은 다르다**: «이 일을 아무도 안 잡았나»(①이 답한다)와 «**이 파일을 남이 잡았나**»(아무 자도 안 묻는다). 규약의 «같은 파일이면 뒤 번호가 기다린다» 는 **뒤엣것**인데 절차에는 그 걸음이 없다. ▸ `check_claim_scope` 는 «제 lock 이 제 범위를 다 적었나» 만 본다 — **살아 있는 lock 둘이 같은 파일을 가리키는 것은 아무도 안 본다**(실측: 그 자는 이 회차 내내 rc 0 이었다). 다음 손이 세울 자리로 적어 둔다. ▸ E 가 «이 등재 글이 갈래 둘을 만들었다» 고 적어 줬는데, 그것이 값을 **아주** 없애지는 않아도 **회차 하나와 번호 하나를 태운 것은 그대로다** — 산 것과 갚은 것을 같이 적는다. ▸ 버린 회차에서 건진 실측 **둘**은 `T507` 행에 보탬으로 올렸다(코드 0줄). | — (아직 임자 없음) | `tools/check_stale_asserts.py` · `.github/workflows/ci.yml` | ROUTINE §2 T508 · T492(다섯을 센 절) · T505·T506(넷을 닫은 절) · 결정 493 · 결정 1379 · 결정 1404 |
| T507 | ⛑ **§1 이 «빨강 진단 전에 돌려라» 고 시키는 자가 오타 난 sha 에 «바뀐 것이 없다» 로 답한다 — 정직한 «깨끗하다» 와 한 글자도 같다**(워커 E · `diff()` 가 git 의 `returncode`·`stderr` 를 버린다 · **T482 와 같은 병** · `--self-test` 아예 없음 = T506 «안 쟀다» ⓐ 가 넘긴 자리) | 🔄 **1회차 push · CI 확인 전(sess-1913-2015 · 워커 E · 16:3X · 결정 1405 · lock `T507` 쥔 채)** — **고침**: `diff()` 가 git 의 `returncode`·`stderr` 를 같이 보고, 실패면 «그 범위를 **못 읽었다** — 이 답은 «깨끗하다» 가 아니라 «모른다» 다» + git 이 한 말 + 받는 꼴을 **딴 줄**로 찍는다. **판정 rc 는 안 건드렸다**(알리는 자 · 결정 493 · T482 가 놓은 꼴 그대로 · 새 규약 0). **자기 검사 갈래 일곱**(T506 «안 쟀다» ⓐ 가 넘긴 자리 · T492 의 다섯 중 마지막) — **제 레포 이력에 안 기대고 임시 git 레포를 세워 돌린다**(진짜 sha 로 세우면 `fetch-depth` 가 그 커밋을 못 담는 날 제 잘못 아닌 까닭으로 빨개지고, 그러면 다음 사람이 갈래를 지운다). **일부러 네 번 부러뜨렸다**(T249) — 고치기 전으로 되돌리기 → ⓐ·ⓕ · git 말 감추기 → ⓐ · 판정 rc 바꾸기 → ⓐ · `BOT` 빼기 → ⓖ. ⚑⚑ **그 부러뜨리기가 둘을 잡았다**: ① 내 자기 검사가 **갈래는 실패하면서 «OK» 를 찍었다**(단언 일부만 이름표에) → `check(label, **조건들)` 로 **묶고** 실패한 조건 이름까지 찍는다 ② `SEARCH` 에서 `BOT`(`Playthrough.cs` · 결정 1072 · 실사고 뒤에 일부러 더한 자리)을 **빼도 여섯 갈래가 다 통과했다** → 갈래 ⓖ. **눈은 안 건드렸다** — 진짜 표본 `62bdebb0` 이 고치기 전과 **같은 2건**이다. **⛑ `ci.yml` 단계까지 같은 커밋에 넣었다** — 글을 쓰는 사이 워커 J 가 **T506 을 닫아 `ci.yml` 을 반납했다**(`e4b47807`). 그래서 «못 넣었다 · 다음 손에게 넘긴다» 로 적어 둔 등재 6항과 결정 1405 ⑧ⓐ 가 **그 자리에서 거짓이 됐고 둘 다 고쳤다**(T501 이 가르친 꼴 · 결과를 박은 문장은 낡는 시한을 안고 있다). 단계는 **본 검사 바로 앞 · 막는 자**로 달았다 — 아래 자는 `continue-on-error` 로 부르는 **알리는 자**라, «알리는 자가 고장 났다» 를 알릴 자리가 그것 말고 없다. ▸ **워커 J 가 T508(같은 파일 · 늦은 등재)에 적어 둔 둘을 그대로 받았다**: ① 공허 방지 → 갈래 **ⓗ**(«0건» 줄이 «훑은 수» 를 같이 말한다) ② «이 자만의 조심 · 한계를 못 박는 갈래» → 갈래 **ⓘ**(흔한 수가 상한을 넘으면 **안 찍고** · 한 자리면 **찍되 판정은 안 한다**). «막는 자로 바꾸지 마라» 도 지켰다(판정 rc 0줄). **갈래 일곱 → 아홉 · 부러뜨림 일곱**(훑은 수 빼기 → ⓗ · 수 상한 999 → ⓘ · 수 상한 0 → ⓒ·ⓘ 둘 다). **확인** = 다음 완주 런 `dotnet` 잡이 초록(이 자는 그 잡의 «보고만» 단계로 이미 돈다 · `ci.yml:171`) + 이 통에서 갈래 일곱 rc=0 ‖ **⊕ 보탬 — 방금 고친 그 병이 «옆문» 에 그대로 있다(sess-1917-23930 · 워커 J · 16:5X · 코드 0줄 · lock 안 잡음 · 새 번호 0개 · 결정 1406)** ▸ ① ⚑⚑ **실측(이 통에서 직접 돌렸다)** — 이 절은 `git diff` 가 죽은 것을 «깨끗하다» 와 갈랐다. **그런데 `git grep` 쪽은 그대로다**: `hits()` 가 부르는 `git grep` 은 **경로가 없으면 rc 1 에 빈손**으로 돌아오고, 그 빈손은 «안 걸렸다» 와 **한 글자도 다르지 않다**. 자 폴더가 없는 트리를 지어 돌려 봤다 → `✓ … 가리키는 자리 0 (자 + 봇 각본) (문자열 1 · 이름 0 · 수 0 를 훑었다)` · **rc 0**. ⇒ **«없다» 와 «못 봤다» 가 이 자 안에서 아직 한 글자인 자리가 남아 있다.** ⚠ 그리고 이 절이 보탠 «훑은 수»(문자열 1)가 그 답을 **더 믿음직해 보이게** 만든다 — 그 수는 **diff 쪽**을 증명하지 grep 쪽을 증명하지 않는다. **고치는 손도 같다**: `git ls-files`(과거 트리면 `ls-tree <at>`)로 `SEARCH` 아래 파일 수를 세어 0이면 «못 쟀다» 로 딴 줄을 찍는다. 판정 rc 를 건드릴지는 이 절이 정하면 된다(결정 493). ▸ ② **갈래 하나가 아직 비어 있다** — 머리글이 제 점수를 «**1적중 · 1소음 · 3놓침**»(`62bdebb0`)으로 적어 뒀는데 갈래 **ⓘ** 가 굳힌 것은 그 **«1소음»** 뿐이다. **«3놓침»**(이름도 값도 그대로인데 **뜻**이 바뀐 자리 — 열 그라데이션·머리 배지·줄 번호)은 아직 **글**이라 자가 바뀌면 조용히 거짓이 된다. «리터럴은 그대로 두고 주석·뜻만 바꾼 diff → 0건» 한 갈래면 «**이 자의 0건은 «옛 단언 없음» 이 아니다**» 가 자와 함께 산다. ▸ ③ **댈 것이 없는 것 둘(적어만 둔다)** — ⓐ 임시 레포로 갈래를 세운 판단이 옳다(제 이력에 기대면 `fetch-depth` 가 짧아지는 날 제 잘못 아닌 까닭으로 빨개진다) · ⓑ 자기 검사 단계를 **`continue-on-error` 없이** 건 것도 옳다 — **알리는 자는 죽어도 원래 조용해서**(정상 상태가 이미 «아무 말 없음» 이다) 자기 검사까지 알리는 자면 «자가 죽었다» 는 소식도 같이 조용해진다. 내가 버린 회차에서 **따로** 같은 결론에 닿았다(둘이 따로 닿은 답이라 더 굳다). ▸ ④ **내가 안 밀었다** — 그 파일은 이 lock 의 것이고 나는 뒤 번호다(T508 은 ✂ · lock 반납). ‖ **⛑ 2회차 push · CI 확인 전(sess-1913-2015 · 워커 E · 17:2X · 결정 1408 · lock 갱신해 쥔 채)** — **워커 J 의 보탬 둘 다 옳아 둘 다 받았다.** ▸ ① ⚑⚑ **내가 «고쳤다» 고 적은 병이 «옆문» 에 그대로 있었다** — 1회차는 `git diff` 쪽만 갈랐고, `hits()` 의 **`git grep` 은 경로가 없어도 rc 1 · stdout 빈손 · stderr 도 빈손**이다(내가 다시 쟀다). **더 나쁜 것은 내가 보탠 «훑은 수» 가 그 절반짜리 0 을 더 믿음직해 보이게 만든 것**이다 — 그 수는 diff 쪽만 증명한다. **증인을 하나만 세우면 그 증인이 안 보는 쪽을 오히려 가린다.** ⇒ **증인 둘**: diff 쪽 «…를 훑어» + grep 쪽 «**자 파일 M개**에서 찾았다». `M==0` 이면 «**찾을 자리를 못 봤다** — «안 걸렸다» 가 아니라 «모른다»» 로 딴 줄(판정 rc 0줄). ⚑ **`git grep` 의 rc 로는 못 가른다 — «안 걸렸다» 도 rc 1 이다.** ▸ ② **갈래 ⓚ — 머리글의 «3놓침» 을 굳혔다**: «리터럴은 그대로 두고 뜻만 바꾼 diff → 0건». 이제 «**이 자의 0건은 «옛 단언 없음» 이 아니다**» 가 **자와 함께 산다**. ▸ ③ **갈래 아홉 → 열하나 · 부러뜨림 열** · `ci.yml` 단계 이름도 «갈래 열하나» 로 맞췄다. ▸ ④ ⚑ **새겨 둔 것 — «같은 병을 고쳤다» 는 «그 병이 사는 자리를 다 셌다» 가 아니다.** **T498 이 이미 적어 둔 줄**을 읽고도 한 손만 고쳤다. 그래서 이번엔 **세어서** 답한다: `subprocess.run` **본체 셋**(`diff`·`hits`·`searched_count`) 중 밖을 **읽는** 손 둘을 **둘 다 갈랐다** ⇒ 남은 손 **0**. ▸ ⑤ **워커 J 의 ③ⓑ 와 따로 같은 답에 닿았다** — 자기 검사 단계를 `continue-on-error` **없이** 건 것: «알리는 자는 죽어도 원래 조용하다». **확인** = 다음 완주 런 `dotnet` 잡에 «옛 단언 자 자기 검사 (갈래 **열하나** · T507)» 가 이름으로 서고 초록 | sess-1913-2015 / 워커 E | `tools/check_stale_asserts.py` · `.github/workflows/ci.yml`(단계 한 개 — T506 이 16:2X 에 반납해 열렸다) — 게임 코드 0줄 · `Assets/` 0파일 · `data/*.json` 0줄 · `aaaw` 0줄 | ROUTINE §2 T507 · **T482**(같은 병을 `ci_test_failures` 에서 가른 절) · **T484**(§1 에 «진단 전에 돌려라» 를 세운 절) · **T492**(자기 검사 없는 자 다섯) · **T505·T506**(그 다섯 중 넷) · 결정 493 · 결정 699 ③ |
| T506 | ⛑ **막는 자 셋이 아직 «자기가 고장 났는지» 를 모른다 — 그리고 그 셋을 «막는 자가 아니다» 로 적어 둔 줄이 있다** (T492 가 «자기 검사 없는 자 다섯» 을 세어 두고 남긴 것 중 T505 가 하나를 집었고 넷이 남았다) | ✅ **종결 — 한 회차 · lock 반납(sess-1917-23930 · 워커 J · 16:2X · 결정 1403·1404)** ▸ **확인을 «단계 이름» 으로 읽었다**(잡이 초록인 것으로 갈음하지 않았다 · 결정 1348 의 그 덫): 런 **1164**(`6a4ec110`) `dotnet` 잡에 **#12 «테스트 using 자 자기 검사 (갈래 열하나 · T506)»** · **#17 «오디오 자 자기 검사 (갈래 아홉 · T506)»** · **#20 «글꼴 글리프 자 자기 검사 (갈래 열둘 · T506)»** 가 저마다 **이름으로 서고 success**, 각각 바로 뒤(#13·#18·#21)가 본 검사다 ⇒ «안 걸면 아무 데서도 안 돈다»(결정 699 · T488)가 이제 **손자국**으로 참이다. ▸ **검수 Q 가 같은 런에서 내 문턱 셋을 실측과 맞대 봤고 댈 것이 없다고 적었다**(`29336d07`) — 그리고 **자기 1회차의 «이 자만 막는 자» 가 틀렸다고 스스로 고쳤다**. ▸ **남은 하나는 새 번호로 등재했다** — `check_stale_asserts`(알리는 자 · `ci.yml:171`)는 **T508**. 글로만 남기면 아무도 안 집는다는 것이 이 절이 고친 바로 그 병이라, 이번엔 **표에 행으로** 세웠다. ‖ (이력) 🔄 **1회차 push(15:4X · 결정 1403)** ▸ **막는 자 셋에 `--self-test` 를 세우고 `ci.yml` 에 걸었다** — `check_audio_webgl`(갈래 **9**) · `check_test_usings`(**11**) · `check_font_glyphs`(**12**). 각각 본 검사 **바로 앞** 한 단계다(T505 가 놓은 자리와 같은 꼴 · 결정 699 ③). ▸ ⚑ **세우고 보니 둘에는 공허 방지가 아예 없었다** — `check_test_usings` 는 길이 어긋나 한 파일도 못 읽어도 «사용 0» 으로 **초록**이었고(막으려던 사고가 «테스트를 한 개도 못 돌리고 죽은 CI #346~#348» 인데도), `check_font_glyphs` 는 **두 방향**으로 거짓이 될 수 있었다: ① 아무것도 안 읽었다 ② cmap 셈이 고장 나 «글꼴이 음절 영역을 다 가졌다» 고 답한다 — **둘 다 화면에서는 똑같이 글자가 폭 0 으로 사라진다**. ②의 문턱은 **잰 값이 아니라 구조적 사실**이다(결정 930): 이 자가 생긴 까닭 자체가 «Jua 는 11,172자를 다 갖지 않았다» 이고 실측은 **2,367자**다. ▸ **일부러 부러뜨렸다(T249) — 셋에 걸쳐 열다섯 번**, 저마다 다른 갈래가 물렸다. ▸ ⚑⚑ **부러뜨리기가 내 자기 검사 자신의 구멍 둘을 잡았다** — ⓐ `check_font_glyphs` 의 «주석은 안 센다» 갈래를 **따옴표 없는** 주석(`// 쩄`)으로 적어 뒀더니, `strip_comments` 를 **통째로 빼도** 그 갈래가 안 울었다(뒤의 리터럴 정규식이 따옴표를 찾으니 그 표본은 **재려던 장치를 아예 안 지났다**). 주석 처리한 코드 한 줄(`// string old = "쩄";`)로 바꾸니 물린다 — **«자가 운다» 를 봤다고 «그 장치를 쟀다» 가 아니다.** ⓑ `check_test_usings` 의 «줄머리» 는 **두 겹**(정규식 줄머리 표식 + `re.match` 자리 물림)이라 한 겹만 풀면 안 운다 — 자의 구멍이 아니라 **지킴이 두 겹**이라는 뜻이다. ▸ **밑동이 문턱을 못 넘는 함정을 나도 한 번 밟았다**(T505 ⓑ 와 같은 꼴) — 가짜 글꼴에 «가·나·다» 셋만 넣었더니 **내가 방금 보탠** 공허 방지 ②(`syll < 100`)에 걸려 갈래 아홉이 전부 «못 쟀다» 로 빨개졌다. ▸ **게이트가 내 손을 잡았다** — `check_py_escapes` 가 내가 머리글에 쓴 `\s` 를 물어 push 를 막았다(파이썬 3.14 면 `SyntaxError`). ⚠ **안 쟀다**(결정 1106 ②): ⓐ `check_stale_asserts`(알리는 자 · `ci.yml:171`)는 **이 절이 안 했다** — 다음 손 몫으로 글자로 남겼다 · ⓑ 이 세 단계가 CI 에서 **실제로 서는지**는 다음 완주 런이 처음 말한다 · ⓒ `check_font_glyphs` 의 ⓚ 갈래만 진짜 Jua 를 읽고, 나머지 갈래는 «cmap 을 잘 읽나» 가 아니라 «읽은 것으로 옳게 판정하나» 를 묻는다(둘은 다른 물음이다). **확인** = 다음 완주 런 `dotnet` 잡에 «오디오 자 자기 검사(갈래 아홉 · T506)» · «테스트 using 자 자기 검사(갈래 열하나 · T506)» · «글꼴 글리프 자 자기 검사(갈래 열둘 · T506)» 세 단계가 **이름으로** 서고 초록. ‖ (이력) 🔄 **선점(15:2X)** — 잡은 까닭은 **실측 하나**다: T505 커밋이 «그중 `check_catalog_keys` 만 막는 자다» 라고 적었는데, `ci.yml` 을 YAML 로 풀어 보면 남은 넷 중 **셋이 `conti…242153 tokens truncated…» 두 버튼이 «광고 보고 티켓 1개»·«다이아 50 으로 티켓 사기» 로 바뀌며, 광고를 보거나 다이아를 내면 티켓이 하나 늘고 버튼이 원래대로 돌아온다. 다음 날 다시 열면 티켓이 2개로 채워져 있다.
- **수치는 전부 표에서**(`Assets/KkomaKnight/dungeon.json` · 카탈로그 «data.dungeon» · 코드 상수 0): `dailyRefill 2` · `gemCost 50` · `adPerDay 1` · `gemPerDay 1` · 던전마다 `first`/`clear`/`sweep` 세 벌. **첫 클리어는 «보너스» 가 아니라 총액**으로 넣었다(주인 «첫 클리어 시 총 펫알 11 + 골드 1000» 을 그대로 적는 편이 나중에 헷갈리지 않는다). ⚠ 주인이 준 값이라 aaaw 원본과 다를 수 있어 `data/*.json` 은 손대지 않았다(T99 5항).
- **규칙(Core/Dungeon.cs · 순수 C#)**: `Roll` 이 날짜가 바뀔 때만 «보유 < 2 이면 2 로» 채우고(2·3 은 그대로 — **더하지 않는다**) 그날의 광고·다이아 횟수를 되돌린다 · `ClaimAd`/`BuyGem` 은 던전별로 하루 1회 · `CanBuyGem` 은 다이아까지 본다 · `Ready`/`AnyReady` 가 빨간 점 판정.
- **화면(EventsScreen)**: 던전 페이지를 열 때 `RollTickets()`(바뀌면 저장) · 카드 제목 띠 «0/2» 하드코딩 → `TicketText(key)` · 빨간 점은 `Ready` 일 때만 · 세부 팝업의 티켓 줄·보상 칸·두 버튼이 표를 따른다 · 광고는 모의 카운트다운 3초(`Overlay.AdCountdown` · T23·T77 과 같은 것) 뒤 티켓 +1.
- **보상 칸이 레퍼런스 21 과 같은 꼴인 이유**: 표에서 «첫 클리어 총액» → «이후 클리어» 순으로 칸을 만들면 지옥의 문이 [펫알 11][골드 1,000][펫알 5][골드 1,000] = **네 칸**이고 앞 두 칸에 «첫 클리어» 배지가 붙는다 — 레퍼런스 21(초록 프레임 4 · 앞 두 칸 FIRST)과 정확히 같아서 배치 표 ⑪ «보상 칸(4개)» 도 그대로다(ui_score 21 불변 · screens 가 찍는 것은 지옥의 문이다). 원정은 두 칸(5,800 첫 · 3,500).
- **안 한 것**: 던전 전투(소탕·도전은 여전히 눌러도 아무 일 없는 껍데기) · 펫알 지갑(결정 264) · 로비·하단 탭 쪽 빨간 점(T96 ⓔ 몫 — 던전 카드의 점만 했다).
- **게이트(로컬 · 전부 초록)**: `dotnet build` 0 경고/0 오류 · `dotnet test` **163/163**(신규 `DungeonTicketTests` 7) · PlayMode 스크래치 컴파일(결정 143) 0 오류 · `gen_meta --check` · `gen_catalog --check`(607) · `check_catalog_keys`(1057/606) · `check_unity_null` 0 · `check_audio_webgl` 20 · `check_data_sync` OK(aaaw `0707999`) · `Sim --seeds 11,12,13` 표 불변(엔진 불변) · **배포 스모크 `--gh-pages` 초록**(에러 0 · 망 4 = T83).
- **확인 수단(다음 회차)**: 이 커밋의 CI 유니티 잡에서 EditMode `DungeonTicketTests` 7 + PlayMode `DungeonTicketPlayTests` Passed + `EventsScreenTests`·`BorderGateTests`·`TextSizeGateTests` 회귀 0 + 그 런의 `screens` 20·21 PNG(카드 «2/2» · 보상 칸 아래 수량 «11»·«1,000»·«5»·«1,000» 이 잘리지 않고 읽히나).

### T69-events 진행 기록 (2026-09-06 · sess-1837-17887 · 워커 C · 계정 1) — 2단계 던전·아레나(20~26) 묶음 코드 push `bda4fa1` · **확인 대기(그 커밋의 CI)**

- **주인이 볼 것 (한 줄)**: 로비 → 던전(하단 탭) 에서 ⓐ 카드 안 **그림 띠가 검은 선으로 끊겨** 보이고 ⓑ 아래 **던전/PvP 탭 두 칸이 각자 검은 외곽선 상자**로 보이며 ⓒ PvP → 입장 → «도전» 팝업의 **상대 5줄**과 «보상» 팝업의 **4줄**이 줄마다 검은 테두리를 두른 상자로 보이면 된 것이다(레퍼런스 20·22·24·25 와 같은 꼴).
- **왜 이 화면인가**: T69 «남은 묶음» 에서 가장 큰 덩어리(던전·아레나 11건)이고 `EventsScreen` 은 어느 lock 범위도 아니었다(T43·T62·T81 ✅ · T63-events ✅ · T72 2단계 3차도 이미 들어와 있다).
- **먼저 안 사실(추측 아님 · CI [#155](https://github.com/kuzuni/aaawunity/actions/runs/34050030434) 유니티 잡 로그의 «[BorderGate] 테두리 없는 칸 목록» 실측)**: 20~26 의 «없음» 11건 =
  `[20]` 카드 그림(`Page:dungeon/Card:hell/Pic`) · 던전/PvP 탭(2칸)(`Page:dungeon/Tabs`) ·
  `[21]` 그 둘(팝업 뒤 페이지가 같이 세어진다) + 티켓 줄(`ui.popup.red/Ticket`) ·
  `[22]` 카드 그림(`Card:arena/Pic`) · 티어 줄(`Card:arena/Tier`) · 탭(2칸) ·
  `[24]` 티켓·전투력 줄(`ui.popup/InfoRow`) · 상대 목록(5줄)(`ui.popup/FoeList`) · `[25]` 보상 목록(4줄)(`ui.popup/RewardList`) · `[23]`·`[26]` 0.
  (로그 원본은 blob 호스트라 이 컨테이너의 프록시가 막지만, MCP `get_job_logs --tail 30000` 이 파일로 떨어져 grep 이 된다 — 결정 172 방식.)
- **재료(주인 에셋 · 코드 도형 0)**: T69 1단계 키 그대로 `fr.rectBorder3`(Ink α0.9 · `BorderMultiplier` 로 프레임 8px = 폰 3px). 새 스프라이트·새 카탈로그 키 0 → `assets-map.md` 추가 줄 없음.
- **한 일** (`Game/EventsScreen.cs` · `Game/BorderAudit.cs` · `Tests/PlayMode/BorderGateTests.cs`):
  ⓐ 던전 카드 2장(`BuildDungeon`)·아레나 카드(`BuildPvp`)의 `Pic` 에 `UiKit.Bordered(pic)` — 아레나는 하늘(`Sky`)을 끼운 «뒤» 에 걸어 링이 맨 앞에 온다.
  ⓑ 바닥 탭(`Foot`)의 `Tab:*/Bg` 에 링 — 아이콘·라벨은 그 뒤에 얹히므로 자동으로 링 위다.
  ⓒ 도전 팝업 `FoeRow:0~4` · 순위 보상 `RewardRow:0~3` 의 **줄 rect 자체**에 링(결정 204 · 조각 `ui.frameDark` 는 NoBorder 라 링이 없었고, 줄 안 프레임 덕에 게이트만 통과하던 상태였다).
  ⓓ 담개 5종은 `BorderAudit.Exempt`(결정 203) · `StrictScreens` 에 `20_dungeon`·`21_dungeon_detail`·`22_arena`·`23_arena_enter`·`24_arena_challenge`·`25_arena_rank_reward`·`26_arena_shop` 추가.
  ⓔ `BorderGateTests` 단언 = 카드 3장의 Pic · 탭 2칸(`HasDarkBorder`) + `FoeRow:0~4`·`RewardRow:0~3`(`AssertUiBarBorder` = 링이 줄 rect 그대로 · 9-slice · 가운데 비움 · raycast 끔 · α ≥ 0.8 · 선 ≥ 8px).
- **안 한 것**: 21 팝업의 «그림 띠»(이름표가 «칸» 이 아니고 팝업 상자가 제 테두리를 가진다) · 24 의 티켓 pill(레퍼런스대로 이미 제 어두운 상자) · 23 순위 줄(T62 의 `ListItem_Ranking` 테두리가 이미 통과) · 배치 표(ref-layout ⑫~⑱)는 한 칸도 안 건드렸다 → `ui_score` 20~26 점수 불변.
- **게이트(로컬 · 전부 초록)**: `dotnet build` 0 경고/0 오류 · `dotnet test` **120/120** · PlayMode 스크래치 컴파일(결정 143) **0 오류** · `gen_meta --check` · `gen_catalog --check`(603) · `check_catalog_keys`(1018/602) · `check_unity_null` 0 · `check_audio_webgl` 20 · `check_data_sync` OK(aaaw `0707999`) · `Sim --seeds 11,12,13` · **배포 스모크 `--gh-pages` 초록**(에러 0 · 오디오 35 = T64 · 망 경고 4 = T83 정상 · fps 40.8) · 줄 끝 `// 주석` 새 줄 0건.
  (이 컨테이너에도 dotnet 이 없었다 — `apt-get update && apt-get install -y dotnet-sdk-8.0` 로 넣었다. `dot.net` 설치 스크립트는 프록시가 막는다.)
- **확인(2026-09-06 19:3X · 같은 세션 · CI [#162](https://github.com/kuzuni/aaawunity/actions/runs/34052939776) `bda4fa1` 실측)**: `BorderGateTests` **Passed** · «[BorderGate] 이름표 128 · 없음 14» 표에서 내 일곱 화면이 전부 **0** = `20_dungeon` 6/6 · `21_dungeon_detail` 10/10 · `22_arena` 4/4 · `23_arena_enter` 1/1 · `24_arena_challenge` 2/2 · `25_arena_rank_reward` 2/2 · `26_arena_shop` 1/1 · strict ✔. 그 런의 PlayMode 빨강 3건(`EventsScreenTests.DungeonArenaPagesAndPopups` «조건 문구» · `RestClearAdTests` · `UiSmokeTests.BattleTicksAndAllBattlePopups`)은 **내 것이 아니다** — 셋 다 T75 1단계(`d24c461`)가 글자 입구에 건 `TextGlyphs.Safe` 로 «·»→«/» · «×»→«x» 가 되었는데 기댓값이 원문 그대로였던 것이고(T75 의 CI #161 은 취소돼 내 런이 그 코드의 첫 완주였다), **T87(워커 A · `31acce2`)이 같은 방식으로 고쳐 CI #166 에서 셋 다 Passed** 로 확인됐다.
- **남은 것 하나**: `screens` PNG 눈 확인(20·22·24·25) — 게이트 통과가 «눈에 보인다» 를 뜻하지 않기 때문이다(결정 184 · T69-forge 회차 3 이 같은 함정을 실제로 겪었다). 지금 `screens` 는 run 155 에 멈춰 있고, 그 사유는 **T91**(클리어 팝업 «골드 카운트업» 단언이 `ItemFrame` 조각의 프리팹 «Text» 를 집는다 · `T69-overlay` lock 범위)뿐이다 — 그 커밋이 초록이 되는 첫 런의 screens 를 보고 이 행을 ✅ 로 닫는다. 내 링은 `UiKit.Bordered` 가 `SetAsLastSibling` 을 하므로 그림·탭 위에 오고(결정 184 조건 충족), 줄 링만 아이콘·글자보다 뒤에 있는데 그 조각들은 줄 가장자리에서 2.5% 안쪽이라 링을 가리지 않는다.
- **다음 T69-* 묶음의 출발점(같은 #162 표)**: 남은 «없음» 은 **14건뿐이고 전부 `LobbyPopups.cs` 세 화면**이다 — `15_quest` 7(트랙 아이콘 1+6칸 `QuestBox/Track/Score` · 새로고침 줄 · 탭 3 `Tab:0~2`) · `11_shop_special` 5(특권 카드 1·2·3 · 카드 설명 상자(2) `Desc:2` · 카드 그림(2) `Pic:2`) · `16_attendance` 2(칸 머리(1칸) `Day:1/Head` · 7일 칸 머리 `Day:7/Head7`). 나머지 화면은 전부 0 이다.

### T69-settings 진행 기록 (2026-09-06 · sess-1828-22161 · 워커 F · 계정 2) — 2단계 설정(12) 묶음 코드 push · **확인 대기(그 커밋의 CI 유니티 잡 PlayMode `BorderGateTests` 설정 구간 + «[BorderGate]» 표 12 + 그 런의 `screens` 12 PNG)**

- **주인이 볼 것 (한 줄)**: 로비 메뉴(≡) → 설정 팝업의 음악·효과음·언어 세 줄이 각각 **검은 외곽선의 띠**로 보이는지(토글·«한국어» 버튼은 원래 외곽선 그대로 · 상자 안 무늬는 전과 같다) · 전투 일시정지 팝업도 같은 코드라 같은 모양.
- **왜 이 화면인가**: T69 «남은 묶음» 의 맨 앞(설정 12 · 3건)이고 어느 lock 범위도 아니다(T63-settings ✅ · T41 ✅ · 팝업 상자 패턴·그라데이션은 T72 2단계 1·4차가 `UiKit.Popup` 에서 이미 깔았다).
- **먼저 안 사실(CI #139 «[BorderGate]» 표 + `screens` run 155 12 PNG)**: 12 의 «없음 3» = «음악 줄»·«효과음 줄»·«언어 줄» 딱 그것(빈 RectTransform + 아이콘 + 라벨 · 배경도 테두리도 없음). 레퍼런스 12 도 줄에 상자는 없지만 ROUTINE T69 2항이 «설정 줄» 을 명시해 링을 넣었다(결정 197).
- **재료(주인 에셋 · 코드 도형 0)**: `fr.rectBorder3`(Ink α0.9 · multiplier 로 8px) + 바탕 `fr.rect`(흰 α0.45) = T69 1단계 키 그대로 · 새 카탈로그 키 없음(assets-map 불변).
- **한 일** (`Game/Overlay.cs` · `Game/BorderAudit.cs` · `Tests/PlayMode/BorderGateTests.cs`): ⓐ `SettingsPopup.Row` 에 `UiKit.Bordered(row, bg: SetRowBg)` — 줄 맨 뒤 바탕 + 맨 앞 Ink 링(가운데 비움 · raycast 끔 · Stretch 0 = 표 ⑨ 불변) · 아이콘 x 1.5% · 라벨 x 12.5%(링 안쪽) · 토글 호스트·언어 버튼은 그 뒤에 만들어져 링 위 ⓑ `BorderAudit.StrictScreens` 에 `12_settings` ⓒ `BorderGateTests` 설정 구간: BGM·SFX·Language 를 `AssertUiBarBorder` + 바탕 맨 뒤 + 아이콘 왼쪽 여백 ≥ 8px + 토글 형제 순서 + `HasPattern(상자)`.
- **안 한 것**: 팝업 상자 자체(조각 제 테두리 · 2항 «상자 자체는 안쪽 칸만») · 링크 글자 2·«데이터 삭제»·«재개/포기» 버튼·«탭하여 닫기»(감사 제외 낱말 · 레퍼런스도 칸이 아님) · 데이터 삭제 확인 팝업(28 · 상자 안 글자·버튼뿐).
- **게이트(로컬 · 전부 초록)**: `dotnet build` 0 경고/0 오류 · `dotnet test` 120/120 · PlayMode 스크래치 컴파일(결정 143) 0 오류 · `gen_meta --check` · `gen_catalog --check` 603 · `check_catalog_keys` 1016/602 · `check_unity_null` 0 · `check_audio_webgl` 20/20 · `check_data_sync` OK(aaaw 0707999) · 줄 끝 `// 주석` 새 줄 0 · 배포 스모크 `--gh-pages` 초록(18:33 UTC · 콘솔 에러 0 · 로비 도달 · 오디오 경고 35 = T64 · 망 경고 4 = T83 정상). dotnet 은 결정 127 방식(`apt-get update && apt-get install dotnet-sdk-8.0`).
- **확인 수단(다음 회차)**: ⓐ 코드 `ec07a49` 의 CI [#160](https://github.com/kuzuni/aaawunity/actions/runs/34052160904)(18:34 UTC 시작 · 이 push 가 T72 5차 `1174888` 의 #159 를 취소했으니 #160 이 그 코드의 첫 완주 런이기도 하다) 유니티 잡 — `BorderGateTests.BattleBarsHaveBordersAndCellTagsAreAudited` Passed(설정 새 단언 포함) + «[BorderGate]» 표 `12_settings` «없음» 0 · strict ✔ ⓑ 그 런의 `screens` 12 PNG 를 `png_crop.py` 로 확대해 줄 3 가장자리의 검은 링(8px = 540px 사본에서 4px) ⓒ 배포 스모크 초록 → T69-settings ✅ · lock 삭제 · 12 점수판은 표 % 불변이라 재채점 불필요.

### T69-shop 진행 기록 (2026-09-06 · sess-1812-29158 · 워커 E · 계정 2) — 2단계 상점(09·10) 묶음 코드 push `d631d86`(+ 주석 정정 `560d604`) · **확인 대기(그 커밋의 CI 유니티 잡 PlayMode `BorderGateTests` 상점 구간 + «[BorderGate]» 표 09·10 + 그 런의 `screens` 09·10 PNG)**

- **주인이 볼 것 (한 줄)**: 상점의 상자 카드 3(큰 신화 상자 · 희귀 · 전설)과 다이아·골드 상품 카드 9 의 가장자리에 다른 화면과 같은 **검은 외곽선**이 있는지(작은 상자 카드 아래 «광고 · 1회 가격» 버튼 줄은 카드 안의 버튼이라 따로 상자가 없다 · 레퍼런스 10 과 같다).
- **왜 이 화면인가**: 2단계의 남은 화면 가운데 어느 lock 범위도 아닌 곳(로비·특권 11 = T78 · 대장간 = T69-forge · 출석 16 = T76 · 펫 = 이 세션이 인계해 종결). T63-shop ✅ · T72 2차(상점 패턴·빛살) ✅ 뒤라 상점 코드가 비어 있었다.
- **먼저 안 사실(CI #155 «[BorderGate]» 표 + `screens` run 155 PNG 4배 확대)**: 09·10 의 감사 대상 7 가운데 «없음» 은 «광고/무료 카드 2개»(= `Box:rare/Bottom`·`Box:legend/Bottom` · T40 의 측정용 빈 rect) 둘뿐 — 나머지 5(대형 배너 · 상자 카드 2 · 상품 카드(1칸) · 상품 카드 2행 · 두 번째 섹션 카드행)는 조각(CardFrame_04 · ListItem_ShopItem 의 중첩 프레임)의 제 외곽선이 «어두움» 판정은 통과하지만 **실측 굵기가 프레임 2~4px(폰 1px 남짓)** 이라 3항 «폰 3px = 프레임 8px» 에 못 미쳤다(결정 184 «없음 0 ≠ 눈에 보인다» 의 상점판).
- **재료(주인 에셋 · 코드 도형 0)**: `fr.rectBorder3`(BasicFrame_Rectangle_01~04_White_Border3 · Ink α0.9 · multiplier 로 8px) = T69 1단계 키 그대로 · 새 카탈로그 키 없음(assets-map 불변).
- **한 일** (`Game/ShopScreen.cs` · `Game/BorderAudit.cs` · `Tests/PlayMode/BorderGateTests.cs`): ⓐ `BuildBigCard`·`BuildSmallCard` 끝에 `UiKit.Bordered(card)` · `BuildPack` 끝에 `UiKit.Bordered(slot)` — 카드 위 맨 앞에 Ink 링 한 장(가운데 비움 · raycast 끔 · Stretch 0 = 표 % 불변) ⓑ `BorderAudit.Exempt` 에 «광고/무료 카드 2개»(결정 195) · `StrictScreens` 에 `09_shop_1`·`10_shop_2` ⓒ `BorderGateTests` 상점 구간: `Content` 의 `Box:*` 3(= gacha.json 상자 수) · `GemPack:0` · `GoldPack:0` 을 `AssertUiBarBorder`(«Border» 자식 · Border 스프라이트 · Sliced · 가운데 비움 · raycast 끔 · α ≥ 0.8 · 선 ≥ 8px · 크기 = 칸)로 · 예외 존재 · `HasPattern`.
- **안 한 것(결정 196)**: 7항 «상점 상품 칸 = ItemFrame» — 레퍼런스 09 의 상품 칸은 세로 카드라 정사각 프레임으로 바꾸면 표 ⑤ 구도가 깨진다 → 카드 위 링으로 대신 · 천막 띠·무료 보급 줄·섹션 헤더·(i)·버튼(감사 제외 낱말 · 레퍼런스도 칸이 아님) · 뽑기 결과 팝업 칸(`GearUi.Cell → DarkFrame` 으로 이미 Ink · screens 에 안 찍힘) · 정보 팝업(공통 팝업 상자 · 안쪽 칸 없음) · 하단 탭 바(마지막 묶음).
- **게이트(로컬 · 전부 초록)**: `dotnet build` 0 경고/0 오류 · `dotnet test` 120/120 · PlayMode 스크래치 컴파일(결정 143) 0 오류 · `gen_meta --check` · `check_catalog_keys` 1014/601 · `check_unity_null` 0 · 줄 끝 `// 주석` 새 줄 0 · 배포 스모크 `--gh-pages --battle` 초록(18:22 UTC). dotnet 은 결정 68 방식(`apt-get install dotnet-sdk-8.0` · 8.0.130).
- **확인 수단(다음 회차)**: ⓐ `560d604` 의 CI 유니티 잡 — `BorderGateTests.BattleBarsHaveBordersAndCellTagsAreAudited` Passed(상점 새 단언 포함) + «[BorderGate]» 표 `09_shop_1`·`10_shop_2` «없음» 0 · strict ✔ ⓑ 그 런의 `screens` 09·10 PNG 를 `png_crop.py` 로 확대해 카드·상품 칸 가장자리의 검은 링(8px = 540px 사본에서 4px) ⓒ 배포 스모크 초록 → T69-shop ✅ · lock 삭제 · 09/10 점수판은 표 % 불변이라 재채점 불필요(가장자리 링은 칸 안쪽).

### T69-forge 진행 기록 (2026-09-06 · sess-1557-2521 · 워커 H · 계정 2) — 2단계 대장간(08) 묶음 코드 push · **확인 대기(그 커밋의 CI 유니티 잡 PlayMode `BorderGateTests` 08 구간 + «[BorderGate]» 표 08 + 그 런의 `screens` 08 PNG)**

- **주인이 볼 것 (한 줄)**: 대장간에서 위 결과 슬롯(초록 테두리 · 모루 그림)과 인벤의 «합성 가능» 초록 칸에도 다른 칸과 같은 **검은 외곽선**이 있는지 · 인벤 바탕에 은은한 무늬가 오른쪽 위로 천천히 흐르는지.
- **재료(주인 에셋 · 코드 도형 0)**: 조각 자체의 `ItemFrame_01_White_Border` 링(T69-gear 와 같음 · Ink α0.9 · multiplier 로 8px) · 패턴 = T72 1단계 `ui.pattern`. 새 카탈로그 키 없음(assets-map 불변).
- **먼저 안 사실(코드 읽기 · CI 표 없이도 확정)**: 08 의 감사 대상 이름표는 «결과 슬롯»·«재료 슬롯» 둘뿐(«안내 문구»·«액션바»·«… 버튼»·«인벤 그리드»는 제외 낱말). 인벤·재료 칸은 T69-gear 의 공용 `GearUi.Cell → DarkFrame` 으로 이미 어둡다. 그러나 `ForgeScreen.GreenFrame`(합성 가능 칸 · 결과 슬롯)이 NormalArea 를 비우고 **초록 변형(`ui.itemFrame.green`)을 새로 스폰**하므로 그 변형의 Border 링은 DarkFrame 을 안 거친 원색(짙은 갈색 · 선 5px)으로 남았다 — 게이트의 «어두움» 판정(밝기 0.13)은 통과해도 선이 8px 규칙에 못 미치고, 주인이 보는 화면에선 초록 칸만 외곽선이 얇다.
- **한 일** (`Game/ForgeScreen.cs` · `Game/BorderAudit.cs` · `Tests/PlayMode/BorderGateTests.cs`): ⓐ `GreenFrame` 끝에 `GearUi.DarkFrame(frame)` 한 줄(결정 167) → 합성 가능 칸·결과 슬롯(모루 칸)도 8px Ink 링 ⓑ `UiKit.PatternBg(Root, PatternTintDark)` — 장비 화면과 같은 값(T72 ① · 화면 적용은 묶음 워커가 같이) ⓒ `BorderAudit.StrictScreens` 에 `08_gear_fuse` ⓓ `BorderGateTests` 08 구간: 같은 키 3개를 만들어(Parts[0]·rar 0 을 하나 더) «합성 가능» 칸이 생기게 한 뒤 결과 슬롯 · 재료 슬롯 0 · 인벤 첫 칸 · 합성 가능 칸(초록 변형 존재 + `AssertItemFrameBorder`) · `HasPattern` 단언.
- **안 한 것**: 액션바(갈색 띠)·안내 문구 상자(`ui.frameDark` 조각 · 자체 외곽선)·«자동»/«합성»/뒤로 버튼(조각 외곽선)·무대(담개) — 감사 제외 낱말이고 레퍼런스 08 도 칸이 아니다 · T72 ② 빛살(대장간엔 «상품 아이콘» 없음) · ③ 그라데이션(공통 조각 · T72 워커) · 하단 회색 띠(탭 바 자리 · 마지막 묶음).
- **게이트(로컬 · 전부 초록)**: `dotnet build` 0 경고/0 오류 · `dotnet test` 113/113 · PlayMode 임시 csproj 컴파일(결정 143) · `gen_meta --check` · `check_catalog_keys` 1018/596 · `check_unity_null` 0 · 줄 끝 `// 주석` 새 줄 0 · 세션 배포 스모크 `--gh-pages` 초록(16:03 UTC · 에러 0 · 로비 · 오디오 경고 42 = T64). dotnet 은 결정 68 방식(`apt-get install dotnet-sdk-8.0`).
- **확인 수단(다음 워커)**: ⓐ 이 커밋의 CI 유니티 잡 — `BorderGateTests.BattleBarsHaveBordersAndCellTagsAreAudited` Passed(08 새 단언 5개 포함) + 로그 «[BorderGate]» 표의 `08_gear_fuse` «없음» 0 ⓑ 그 런의 `screens` 08 PNG 를 `python3 tools/png_crop.py` 로 확대해 초록 칸·모루 칸의 검은 외곽선 ⓒ 배포 스모크 초록 → T69-forge ✅ · T63-forge 의 남은 «장착중» 눈 확인도 같은 PNG 로 한 번에.

### T69-gear 진행 기록 (2026-09-06 · sess-1542-8871 · 워커 G · 계정 2) — 2단계 장비·세부(06·07) 묶음 코드 push · **확인 대기(그 커밋의 CI 유니티 잡 PlayMode `BorderGateTests` + «[BorderGate]» 표 06·07 + 그 런의 `screens` 06·07 PNG)**

- **주인이 볼 것 (한 줄)**: 장비 화면에서 공격력·체력·실드 3칸, 장착 슬롯 6칸과 인벤 칸(등급색 속 + **검은 외곽선** · 폰에서 3px 안팎), 장비를 눌러 뜨는 세부 팝업의 «슬롯 Lv»·«부위» pill · 스탯 상자 · 옵션 줄 · 비용 줄, 빈 슬롯 팝업의 «+» 칸에 검은 테두리가 생겼는지 · 인벤 바탕에 은은한 무늬가 오른쪽 위로 천천히 흐르는지.
- **재료(주인 에셋 · 코드 도형 0)**: 사각 칸 = `fr.rectBorder3`(1단계 · Ink α0.9 · 8px) · 아이템 칸 = 조각 자체의 `ItemFrame_01_White_Border`(카탈로그 `fr.itemBorder` · 79×79 · 9-slice 39/40 · 선 5px 실측)를 Ink 로 tint · 패턴 = T72 1단계 `ui.pattern`. 새 카탈로그 키 없음(assets-map 불변).
- **먼저 안 사실(CI #119 «[BorderGate]» 표)**: 06_gear 12행 중 없음 4 = «장비 무대(캐릭터+슬롯)» + 스탯 3칸 · 07_gear_detail 16행 중 없음 8 = 위 4 + «이름줄» + 메타줄 pill 2 + «비용줄». 슬롯 6·인벤 칸은 이미 «있음» 이었는데 이유가 등급 변형 프리팹의 Border 가 짙은 갈색(밝기 0.13)이라서였고, 선은 5px(폰 1.9px)라 8px 규칙엔 못 미쳤다(결정 163).
- **한 일**: ⓐ `GearUi.DarkFrame(frame, scale)` — 프레임 아래 «Border» Image(스프라이트 이름 `ItemFrame_01_White_Border*`)를 전부 Ink α0.9 + Sliced + multiplier(5 ÷ (8 ÷ scale)) · `Cell`(인벤·대장간·뽑기 결과·세부 아이콘 칸 공용) · `GearScreen.Refresh`(슬롯 6 · 변형 재스폰 뒤마다) · `OpenSlot` 의 Empty 에서 부른다 ⓑ `GearScreen.StatCell` 에 `UiKit.Bordered` ⓒ `GearUi.Pill` 에 `UiKit.Bordered` → Pill1·Pill2·Stats·Opt:i·Cost 전부 ⓓ 빈 슬롯 팝업 아이콘 칸 = `ui.itemFrame.empty`(Add_1 «+» · Item·Lock·Focus·Disable·Add_2 끔 · `FitScale` 로 표 ④ 아이콘 칸 크기) ⓔ `BorderAudit`: `06_gear`·`07_gear_detail` strict + 예외 «장비 무대(캐릭터+슬롯)»·«이름줄» ⓕ `UiKit.PatternBg(Root, PatternTintDark)` 장비 화면 바탕 ⓖ `BorderGateTests`: 06 = 스탯 3칸 `HasDarkBorder` · 슬롯 6 + 인벤 첫 칸 `AssertItemFrameBorder`(켜진 ItemFrame Border 링 ≥ 1 · Ink · α ≥ 0.8 · Sliced · 선 ≥ 8px(축소 배율 보정)) · `HasPattern` / 07 = Pill1·Pill2·Stats·Cost(+Opt:0 있으면) `HasDarkBorder` · IconSlot `AssertItemFrameBorder` / 빈 슬롯 팝업(부위 0 해제 뒤 `OpenSlot`) = «IconSlot/Empty» 존재 + 같은 단언.
- **안 한 것(다음 묶음 몫 · 결정 155 규칙 그대로)**: 하단 탭 바(전 화면 공용 · 마지막 묶음) · «상점»·«대장간» 버튼(조각에 이미 외곽선) · 등급 배지(Title_01 명판 조각) · 상단 재화 pill(T69-lobby 가 TopBar 공통으로 이미) · 대장간(08)의 같은 Cell 은 `DarkFrame` 이 공용이라 따라 어두워지지만 08 의 strict·선택 칸(초록 프레임)·모루 칸은 T69-forge 몫 · T72 ③ 그라데이션(버튼·명판 공통 조각 · T72 워커).
- **게이트(로컬 · 전부 초록)**: `dotnet build` 0 경고/0 오류 · `dotnet test` 100/100 · PlayMode 임시 csproj 컴파일 0 오류(결정 143 · `BorderGateTests` 새 단언 포함) · `gen_meta --check` · `gen_catalog --check` 595 · `check_catalog_keys` 1085/594 · `check_unity_null` 0 · `check_audio_webgl` 20 · `check_data_sync .aaaw-src` OK(aaaw `0707999`) · `Sim --seeds 11,12,13` 표 그대로(엔진 불변) · **배포 스모크 `tools/webgl_smoke.sh --gh-pages` 초록**(15:53 UTC · 콘솔 에러 0 · 로딩 완료 · 로비 도달 · 오디오 경고 42 = T64) · 줄 끝 `// 주석` 새 줄 = 한 문장 줄뿐 0 위험. dotnet 은 결정 127 방식(`apt-get install dotnet-sdk-8.0`)으로 세션 안 설치.
- **확인 수단(다음 워커)**: 이 커밋이 든 CI 유니티 잡 — ⓐ PlayMode `BorderGateTests.BattleBarsHaveBordersAndCellTagsAreAudited` Passed(빨가면 메시지에 어느 칸·어느 계약인지 그대로 나온다 · «ItemFrame 테두리 선 ≥ 8px · 지금 N» 이면 결정 163 ⓑ 의 배율 계산을 본다) ⓑ 로그 «[BorderGate]» 표에서 `06_gear`·`07_gear_detail` 행의 «없음» = 0 ⓒ 그 런의 `screens` 06·07 PNG 를 `Read` 로 «검은 외곽선이 보이나»(띠로 찍히면 `tools/png_crop.py --strip`) ⓓ 배포 스모크 ⓔ `ui_score.py 06_gear`·`07_gear_detail` 표 점수 불변(테두리는 칸 안쪽). 초록이면 이 행을 ✅ 로 바꾼다 — `T69-gear.lock` 은 이 세션이 지웠다. 코드 커밋 `f25cd1a` 의 메시지에 적힌 «결정 157» 은 **163** 을 뜻한다(push 직전 rebase 에서 다른 워커들의 157~162 와 번호가 겹쳐 문서에서만 163 으로 정정 · CI #131 https://github.com/kuzuni/aaawunity/actions/runs/34043949274).

### T81 진행 기록 (2026-09-06 · sess-1429-29121 · 워커 B) — 코드 `d0f4f75` · **확인 대기(그 커밋의 CI PlayMode `ArenaDummyNumbersAreFilled` + screens 23·24 PNG)**

- **주인이 볼 것 (한 줄)**: 아레나 순위 목록(23)의 4~10위와 도전 팝업(24)의 상대 5줄에 ⚔ 전투력·🏆 승점이 «0»·«—» 대신 실제 숫자로 뜨고, 위 순위일수록 큰지 — 내 줄(시상대 가운데)은 지금 내 전투력 그대로다.
- **값 규칙**(전부 `Assets/KkomaKnight/arenaDummy.json` · 코드엔 식만): 전투력 = 내 전투력 × 계수(1위 `top` 1.6 → 내 순위 `me` 1.0 → `bottomRank` 20 에서 `bottom` 0.6 · 그 아래는 0.6 으로 누움) · 승점 = 1위 2,400 에서 순위마다 40~60 단조 감소(0 에서 정지). 시드는 순위 하나뿐이라 화면을 다시 열어도 같은 값이다.
- **왜 흔들림을 «계단 폭» 에 곱했나(결정 166)**: 지시서대로 계수에 ±5% 를 곱하면 순위 사이 계단(me 1.0 → bottom 0.6 을 19칸에 나누면 ≈2.1%)보다 흔들림이 커서 **아래 순위가 위 순위보다 세지는 뒤집힘**이 난다. 그래서 흔들림을 계단 폭에 곱해 쌓았다 — ±5% 는 그대로 살아 있고 단조는 깨지지 않는다(EditMode `PowerAndScore_FallWithRank` 가 40위까지 단언).
- **`UiKit.FmtComma` 를 새로 둔 이유(결정 167)**: 지시서가 «천 단위 콤마(`UiKit.Fmt`)» 라 했지만 `Fmt` 는 1만 이상을 «11.3K» 로 줄인다. 레퍼런스 23·24 는 «11253»·«5009» 처럼 전 자릿수를 보여 준다 → 아레나 숫자에만 콤마 전용 포맷을 쓴다(상단 재화 바는 `Fmt` 그대로).
- **24 상대 이름(결정 168)**: 지시서 «이름은 지금 «도전자 N» 유지» 인데 도전 팝업 5줄은 실제로 이름 자리가 «—» 였다 → 23 과 같은 «도전자 N» 으로 채웠다(숫자만 채우고 이름을 «—» 로 두면 반쯤 만든 화면으로 보인다). 새 콘텐츠가 아니라 같은 껍데기 이름 규칙이다.
- **안 한 것**: 실제 아레나 시스템(도전·보상·순위 갱신)은 여전히 없다 — 표시용 더미다. 밸런스·`data/*.json`·자리·크기·색 불변(T63-events 규격 그대로).
- **게이트(로컬 · 전부 초록)**: `dotnet build` 0 경고/0 오류 · `dotnet test` **120/120**(신규 `ArenaDummyTests` 7) · PlayMode 임시 csproj 컴파일 0 오류(결정 143) · `gen_meta --check` · `gen_catalog --check`(598) · `check_catalog_keys`(1019/597) · `check_unity_null` 0 · `check_audio_webgl` 20 · `check_data_sync` OK(aaaw `0707999`) · `Sim` 시드 11,12,13 표 그대로 · **배포 스모크 `--gh-pages` 초록**(에러 0 · 로비 · 오디오 경고 42 = T64).
- **✅ 확인 결과(16:5X UTC · 같은 세션 · CI #137 = 이 커밋 자신)**: PlayMode **`ArenaDummyNumbersAreFilled` Passed**(23 순위 7줄 + 시상대 배너 3 + 24 상대 5줄의 ⚔ 전투력·🏆 승점이 «0»·«—» 이 아니고 천 단위 콤마 · 순위대로 단조 · 아래 순위 < 내 전투력 · 오른쪽 위 = 내 실제 전투력) · EditMode **120/120**(`ArenaDummyTests` 7 전부 Passed). 그 런의 빨강 3건은 전부 남의 행(T69/T78 BorderGate · T72 UiTexture · 로비 스모크). **`screens` 23·24 PNG 눈 확인은 못 했다** — 유니티 잡이 계속 빨개 `screens` 가 run 106 에 멈춰 있다(다음 초록 런에서 보면 된다).
- **확인 수단(기록용)**: `d0f4f75` 가 든 CI 유니티 잡 — ⓐ PlayMode `EventsScreenTests.ArenaDummyNumbersAreFilled` Passed ⓑ EditMode `ArenaDummyTests` 7 Passed ⓒ 그 런의 `screens` 23·24 PNG 를 `Read`(순위 목록·도전 줄에 숫자) ⓓ 배포 스모크.

## 주인 콘솔 에러 보고함 (주인이 붙인 원문 — 다 고칠 때까지 남긴다 · 워커는 매 세션 읽고 작업으로 올린다)

> 주인 상시 지시(2026-09-05): **플레이하면 콘솔에 빨간 에러가 항상 뜬다. 루틴이 매번 플레이 상태를 검증해 다 고쳐라. 다른 에러도 있는지 전부 확인해라.** 새 로그는 아래에 번호를 이어 붙이면 된다(원문 그대로).

| # | 원문(발생 상황) | 상태 | 작업 | 원인/커밋 |
|---|---|---|---|---|
| ① | `Renderer2D Pass: Fake or uninitialized surface is not supported for attachment 0.`<br>`UnityEngine.Rendering.RenderPipelineManager:DoRenderLoop_Internal (UnityEngine.Rendering.RenderPipelineAsset,intptr,UnityEngine.Object,Unity.Collections.LowLevel.Unsafe.AtomicSafetyHandle)` — 에디터 플레이 중 반복 | ✅ 수정 (`2203550`) — 주인 에디터 재확인 대기 | T12 | **확정**: `HeroView.BuildStage` 가 `new RenderTexture(512, 512, 0, ARGB32)`(깊이 0 · `depthStencilFormat None`) 을 런타임 카메라 `targetTexture` 로 썼다. 프로젝트의 유일한 렌더러 `Renderer2D.asset` 은 `m_UseDepthStencilBuffer: 1` 이라 렌더그래프가 카메라 타깃의 깊이 표면을 attachment 로 import 하는데 그 표면이 없어(«Fake or uninitialized surface») 렌더패스 시작이 실패한다. 이 카메라는 로비·장비 화면에서 매 프레임 그리므로 «항상» 뜬다. 수정 = `HeroView.CreateTargetTexture`(깊이 24 + `GetDepthStencilFormat(24, 8)` = D24S8/D32S8) + `UniversalAdditionalCameraData`(Base) 명시. 회귀 방지 = `HeroViewTests.StandaloneHeroViewRendersWithoutErrors`(depth>0 · depthStencilFormat≠None 단언 + `Camera.Render()` 3프레임 · 빨간 줄 0) — **CI #33 에서 Passed · 로그에 이 에러 문자열 0건**. |
| ② | `EndRenderPass: Not inside a Renderpass`<br>`UnityEngine.Rendering.RenderPipelineManager:DoRenderLoop_Internal (…)` — ① 과 짝으로 매 프레임 | ✅ 수정 (`2203550`) — 주인 에디터 재확인 대기 | T12 | ① 과 같은 원인(렌더패스 시작이 실패한 뒤 End 가 불려 짝이 안 맞음) — ① 이 없어지면 같이 사라진다. 추가로 `HeroView.OnDestroy` 순서를 «카메라 끔 → targetTexture null → RawImage 끔 → 무대 파괴 → Release → Destroy» 로 고정해 해제된 텍스처를 파이프라인이 만지지 않게 했다. |
| ③ | (주인: «다른 에러들도 있는지 다 확인해서 고쳐라» — 위 둘 외 미기재 항목 전수 감사) | ✅ 감사 완료 (`2203550` · 결과는 T12 완료 기록) — 새로 확인된 빨간 줄 원인 0 · 재발 방지 게이트 2개(`check_catalog_keys.py` · PlayMode 왕복 테스트) | T12 §3 | 감사 결과: 실제 플레이에서 빨간 줄을 내는 곳은 ①② 뿐. 나머지는 «데이터/에셋이 빠졌을 때만»(카탈로그 미연결 · 프리팹 자식 경로 오타 → 노란 경고) 이거나 DOTween safeMode(켜짐 · `useSafeMode: 1`)가 조용히 kill 하는 트윈. 프리팹 경로 경고는 T11 스모크 테스트가 화면마다 잡는다. |
| ④ | (주인 폰 Chrome 스크린샷 2026-09-06 · gh-pages `20b11aa` 첫 화면) `RangeError: Maximum call stack size exceeded` — `invoke_iii (KkomaKnight.framework.js:9:473704)` → `wasm-function[147169]` → `wasm-function[147187]` → … 반복 | ✅ **종결 — 원인 미확정 · 재현 안 됨 · 주인이 폰에서 «잘 된다» 확인(2026-09-06 11:3X UTC · 같은 배포 `20b11aa`)** (결정 120) | T59 | 재현 시도 8종(G) + 독립 재현 2종(E · 모바일 UA) + 워커 D 의 `webgl_smoke.sh --gh-pages`(배포 `afa102d` · errors=0 · loaded=true) 가 전부 초록 · **고친 커밋은 없다**(코드 수정 없이 닫는다) · 재발하면 새 번호로 열고, 그때는 진단 빌드(`2cd0eb1` · 심볼 Embedded)가 배포돼 있어 이름 있는 스택이 바로 나온다 · 근거 = T59 진행 기록 |

27. **투사체 표적 규칙(T21 · 구 T14) — 종결(원본이 이미 무작위 · `39f4d1b`).** sim.js `randTarget` = 살아 있는 적 중 플레이어 앞 −30~540px 안에서 균등 무작위(맨 앞만 ×) · 도끼·화살·번개가 발마다 새로 고른다 · 창·검기는 관통형이라 앞에서부터(원본 규칙). 우리 엔진이 줄 단위로 같아 **규칙 변경·골든 재생성 없음**. 참고: 540px 는 거리 기준이라 다음 웨이브 선두가 그 안에 있으면 그쪽도 맞을 수 있다(원본 그대로) — «반드시 현재 웨이브 안으로만» 을 원하시면 규칙 변경(골든 재생성)이라 한 줄로. «맨 앞만 맞는 것처럼» 보이는 것은 적 간격 44px 가 화면에서 0.66u 라 한 웨이브가 겹쳐 보이기 때문 — 24번(간격 2배)과 같은 결정.
28. **클리어 팝업 «그냥 받기» 버튼(T23 · 구 T16) — 기본값으로 반영(`9ea5d9d`).** «광고 보고 보상 ×2» 만 두면 광고 없이는 못 나간다. Play_Result_Win_01 프리팹의 «Home» 버튼 자리를 «그냥 받기»(1배 · 로비로)로 썼다(작은 글자 버튼을 새로 그리지 않고 프리팹 버튼 그대로). ×2 의 «2배» 는 이 판의 골드 전부(처치 골드 + 클리어 보너스)를 한 번 더 주는 것 — 클리어 보너스만 2배를 원하시면 한 줄. 빼길 원하면 한 줄.

29. **장착 중 장비를 합성 재료로 쓸 때(T24)** — 재료로 사라진 장착분의 슬롯은 «결과물이 같은 부위면 그것을 장착, 아니면 빈 슬롯» 을 기본값으로 둔다(자동 장착 금지 원칙의 유일한 예외 · 결과물은 재료보다 항상 좋다). 그냥 빈 슬롯으로 두길 원하면 한 줄. **→ T24 `920fe0b` 에 이 기본값 그대로 구현**(`GearSystem.ReEquipAfterFuse` 한 곳 · 수동·자동 공용 · 빈 슬롯을 원하시면 그 함수의 «같은 부위» 분기 한 줄만 지우면 된다).

30. **오디오 에셋 출처(T28)** — 이 환경은 kenney.nl·opengameart.org·freesound.org 접속이 막혀 있어(프록시) GitHub 에 미러된 CC0 팩만 받을 수 있다. GitHub 에서도 못 구하면 시스템만 만들어 두니, 주인이 원하는 팩을 `Assets/Audio/` 에 넣어 주면 카탈로그 한 줄로 붙는다. **→ 종결(T28 `9c1eb54`): GitHub 미러에서 CC0 20개를 구해 넣었다**(Kenney 16 · Juhani Junkala 2 · SketchyLogic 1 · `Assets/Audio/LICENSES.md`). 전투 곡은 맵 4종 공용 1곡(테마별 4곡은 CC0 루프를 4개 못 구함) — 원하시면 곡 파일만 주시면 `bgm.battle.<테마>` 로 붙인다. 소리를 바꾸려면 같은 이름 `.ogg` 로 덮어쓰기.
104. **T58 원인 = `PlayShot.Save` 의 `cam.CopyFrom(world)` 가 `WorldCam` 의 letterbox 뷰포트(`cam.rect` · WorldCam.cs:36 · 배치 모드 가로 화면에선 프레임 폭 ≈35%)까지 복사 → 촬영 카메라가 RT 의 35% 띠에만 그리고 캔버스(ScreenSpaceCamera)가 그 pixelRect 에 맞춰 프레임을 188×407 로 줄임(관측 188×404 와 일치) · 스프라이트 겹침 = 카메라 모드 캔버스(order 10)가 월드 스프라이트(order ≤ 350 · Fx)와 같은 «Default» 층에서 order 로 겨룸** (T58 · `0a036a3` · sess-1007-29927 · 워커 G) — 수정은 촬영 코드만: `cam.rect = (0,0,1,1)` · 촬영 중 `canvas.sortingOrder = short.MaxValue`(끝나면 10 복원) · CanvasScaler 는 손대지 않음(scaleFactor 가 Screen 기준으로 남아도 캔버스 rect = pixelRect/scale 이라 프레임(FitInParent · 같은 비율)이 꽉 찬다 — 균일 배율이라 % 불변). 확정 근거는 CI 로그의 `[PlayShot] NN: cam.rect=… fill=…` 한 줄(0.95 미만이면 UiShotsTests 가 빨강). ROUTINE T58 의 다른 후보(2패스 UI 카메라 · ConstantPixelSize)는 쓰지 않았다 — rect 한 줄이 원인이라. 되돌리려면 `PlayShot.Save` 의 두 줄.
105. **T40 lock 인계 + 비평 회차 1 = 09/10 둘 다 10.0 ✅ — D(sess-0750-20344) 의 lock(08:58) 이 90분을 넘겼고 «08:55 재기동해 채점» 예고가 실행되지 않아 규약대로 인계했다(파일을 내 SID 로 덮어 커밋·push) · 채점 재료는 D 가 기다리던 그 screens 첫 배포(CI #83) 그대로** (T40 · sess-1007-29927 · 워커 G) — 카드 프레임 조각(CardFrame_04)의 왼쪽 위 색 탭은 레퍼런스에 없지만 «그림·색 = 점수 밖» 규칙으로 감점하지 않고 선택 처방으로 남겼다(T43 회차 1 의 26 상인 카드 «띠 조각» 은 카드 밖으로 삐져나온 잘림이라 감점 · 여기는 카드 안 장식이라 아님 — 기준 구분). 되돌리려면 점수판 09/10 줄과 T40 행.
107. **CI #87 빨강 1건 = 내 편집 실수 — 한 줄에 문장이 여럿인 줄(EventsScreen.cs:281 · `Icon; Pct; Clickable; Tag;`) 의 첫 문장만 old_string 으로 바꾸며 뒤에 `// 주석` 을 붙여 나머지 3문장을 주석 처리했다** (sess-1007-29927 · 워커 G · 수정 `afa102d`) — dotnet 게이트는 «주석이 된 문장» 을 못 잡는다(컴파일은 되고 PlayMode 만 잡음). 규칙으로 ROUTINE §1 에 한 줄: **이 레포는 한 줄에 문장 여럿을 두는 코드가 많으니, 줄 끝에 // 주석을 붙이기 전에 그 줄의 나머지를 확인하고 주석은 윗줄에 둔다** · 자기 커밋을 push 전에 `git show <commit> -- Assets | grep -E '^\+' | grep -E ';\s*//'` 로 «문장 뒤 // 가 붙은 새 줄» 을 훑는다. 되돌릴 것 없음(수정 완료).

## 주인 할 일

- README «내가(주인) 할 일» 5단계 (활성화 워크플로 → .alf → .ulf → 시크릿 3개 → Pages 소스 gh-pages).

## 게이트 현황 스냅샷 — T5 완료 직후

| 게이트 | 결과 |
|---|---|
| `dotnet build tools/dotnet/KkomaKnight.sln -c Release` | 0 경고 · 0 오류 (Core · Game · Tests · Sim) |
| `dotnet test tools/dotnet/Tests` | 40/40 (NUnit 3.6.1 — 유니티 포크와 같은 API 면 · 3.5.0 은 net8 어댑터가 테스트를 못 찾는다) |
| 유니티 CI (#14) | EditMode 32/32 · PlayMode 1/1 · WebGL → gh-pages · Android APK 아티팩트 (T5 커밋의 CI 는 푸시 뒤 확인) |
| `python3 tools/gen_meta.py --check` | 초록 |
| `tools/check_data_sync.sh` | OK — aaaw main `c7ebe37` 과 동일 (`sim.js@0618225…`) |
